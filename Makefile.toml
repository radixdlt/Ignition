[config]
default_to_workspace = false

[tasks.test]
command = "cargo"
args = [
    "test",
    "--release",
    "--features",
    "package-loader/build-time-blueprints",
    "--no-fail-fast",
]

[tasks.check]
command = "cargo"
args = ["check", "--all-targets", "--target-dir", "target/example"]

[tasks.format]
install_crate = "rustfmt"
command = "cargo"
args = ["fmt"]

[tasks.fmt]
alias = "format"

[tasks.publish-stokenet]
command = "cargo"
args = [
    "run",
    "--package",
    "bootstrapper",
    "--",
    "publish-test-to-stokenet",
    "./frontend/public/config.json",
    "./frontend/src/config.json",
]

[tasks.frontend-start]
command = "npm"
args = ["start", "--prefix", "./frontend"]
