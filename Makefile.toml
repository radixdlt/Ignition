[config]
default_to_workspace = false

[tasks.test]
install_crate = "nextest"
command = "cargo"
args = ["nextest", "run"]

[tasks.check]
install_crate = "clippy"
command = "cargo"
args = ["clippy"]

[tasks.format]
install_crate = "rustfmt"
command = "cargo"
args = ["fmt"]